CREATE TABLE THING (ID BIGINT NOT NULL, FILESIZE BIGINT, MD5 VARCHAR(255), FILENAME VARCHAR(255), LASTMODIFIED DATETIME, TAKEN DATETIME, PRIMARY KEY (ID))
ALTER TABLE THING ADD CONSTRAINT UNQ_THING_0 UNIQUE (fileName)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
